import { Modal } from "react-bootstrap";
import React from "react";

const Login = () => {
  return (
    <div id="main">
        <Modal show={true}>
            <h1>This is a modal</h1>
        </Modal>

    </div>
  );
};

export default Login;